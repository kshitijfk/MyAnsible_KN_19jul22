---
- name: grant tomcat ownership on tomcat folder
  execute: sudo chown -R tomcat:tomcat /opt/tomcat/

- name: change to execute permissions on bin folder
  execute: sudo chmod -R u+x /opt/tomcat/bin